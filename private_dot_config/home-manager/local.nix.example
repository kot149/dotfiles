{ pkgs, ... }:

{
  home.packages = with pkgs; [
    # list extra packages here
  ];

  localDeny.packages = with pkgs; [
    # list unwanted packages here
  ];

  localAllowUnfree.packages = [
    # list extra unfree package names to allow (matched via lib.getName)
  ];
}
