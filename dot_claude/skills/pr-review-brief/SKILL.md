---
name: pr-review-brief
description: >-
  PRレビューを引き受けたときに、レビュー開始前の背景知識ブリーフを作るスキル。
  PR本文・紐づくJiraチケット（親エピック・関連チケット含む）・リポジトリ内の設計資料を
  並行して収集し、PRの位置づけ・機能全体像・用語集・既知の意図的な負債・
  レビューで確認すべき不一致をまとめて日本語で報告する。
  「PR #NNN のレビューを頼まれたので背景を説明して」「レビューの準備をして」
  「このPRの前提知識をまとめて」「review brief」などの依頼で使用する。
  コードのdiffレビュー自体は行わない（それは /code-review 等の担当）。
---

# PRレビュー準備ブリーフ

レビュー対象PRの背景知識を収集し、レビューアが diff を読み始める前に必要な文脈を1つのブリーフにまとめる。

## 入力の確定

1. PR番号（と必要ならリポジトリ）を特定する。番号だけ渡された場合、カレントディレクトリ（メタリポジトリなら該当サブリポジトリに `cd`）の `gh` で解決する。
2. 曖昧なら `gh pr list --search` で候補を出してユーザーに確認する。

## Step 1: PR情報の取得（自分で実行）

```bash
gh pr view <番号> --json title,body,author,baseRefName,headRefName,files,additions,deletions,url,commits
```

`jq` で整形して読む。本文から以下を抽出する:

- 紐づくJiraチケットキー（`browse/XXX-NNN` 形式のURL）。本文中に複数出てくる関連チケットもすべて控える
- マージ先ブランチ（develop直行か統合ブランチか）
- 本番影響の有無、リリース種別
- PR本文が参照する設計資料のパス（`docs/`、`specs/` など）
- PR本文中の専門用語・略語（QD-x、スライス番号 S0〜S7 のような社内記法）

## Step 2: 並行調査（subagentに委譲）

**2つのagentを1メッセージで同時に起動する。**

### Agent A: Jira調査（general-purpose）

promptに含めること:

- Atlassian MCPツールは ToolSearch で `select:mcp__claude_ai_Atlassian__getJiraIssue,mcp__claude_ai_Atlassian__searchJiraIssuesUsingJql` （必要なら getAccessibleAtlassianResources も）をロードしてから使う、と明記する
- 本体チケット: 本文と全コメントを取得
- 親エピック・関連チケット: 本文または概要を取得（関連は1〜2行の位置づけで十分と指示）
- チケット中の専門用語・略語の定義を抜き出すよう指示
- 「取得できない場合はJQLで探す。推測で捏造しない」と明記する

### Agent B: リポジトリ設計資料調査（Explore）

promptに含めること:

- 対象リポジトリの絶対パスと、PRのhead/baseブランチ名
- **ワークツリーのHEADは古い可能性があるため、`git show origin/<PRブランチ>:<path>` でPRブランチ上のファイルを読む**よう明記する（fetchが必要なら先に `git fetch origin <branch>`）
- 調べる対象:
  - サービス・モジュールの全体像（README、`docs/architecture/` 等）とレイヤ構成・依存ルール
  - PR本文が参照する設計ドキュメント・仕様書
  - PRが導入する型・関数の依存先となる既存の型定義や既存パターン（PR本文で「〜と同型」とされる既存コード）
  - エラーハンドリングやルーティングなど、PRの変更が組み込まれる仕組み
- 「ファイル全文の貼り付けは不要。概念と契約を説明できる粒度で、`path:line` 参照付きでまとめる」と指示する

## Step 3: ブリーフの作成

両agentの結果とPR本文を突き合わせ、以下の構成で日本語のブリーフを書く:

1. **PRの位置づけ**: 何を追加するPRか、規模、作者、本番影響、マージ先、到達可能性（配線済みか未配線か）などレビューの重みを決める前提
2. **機能全体像**: 親チケットが目指す機能と、その中でこのPRが担うスライス・フェーズ
3. **PRの構成要素**: ファイル→役割の対応表と、主要な設計判断・契約変更
4. **用語集**: PR本文・チケット・コードに出てくる専門用語と社内略語。出典が見つからなかった略語は「見つからなかった」と明記する
5. **既知の意図的な負債**: PR本文やコード内で自己申告済みの制限・切り出し先チケット。「レビューで指摘しても新規指摘にならないもの」として列挙する
6. **レビュー時に確認したい不一致**: PR本文・チケット・実コード・OpenAPI例の間で食い違う記述があれば挙げる（調査中に見つかったもののみ。無理に作らない）

## 注意

- このスキルはdiffレビュー自体はしない。ブリーフ提示後、「続けてdiffレビューに進むか」を一言添えて終える
- 情報源間の食い違い（PR本文 vs 実コードなど）は握りつぶさず、Step 3-6 に必ず載せる
- agentの報告は鵜呑みにせず、明らかに矛盾する点は元データ（PR本文・チケット）と照合する
